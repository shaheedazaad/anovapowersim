% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_curve.R
\name{power_n}
\alias{power_n}
\title{Search for the sample size needed for target ANOVA power}
\usage{
power_n(
  between = NULL,
  within = NULL,
  term,
  target_pes,
  power = 0.8,
  n_sims = 10000,
  alpha = 0.05,
  n_start = NULL,
  n_max = 1000,
  tol = 0.01,
  gpower = FALSE,
  progress = interactive(),
  parallel = FALSE,
  cores = NULL,
  seed = NULL
)
}
\arguments{
\item{between}{Named integer vector of between-subject factor level counts,
e.g. \code{c(group = 2)}. Use \code{NULL} for no between-subject factors.}

\item{within}{Named integer vector of within-subject factor level counts,
e.g. \code{c(time = 3, condition = 4)}. Use \code{NULL} for no within-subject
factors.}

\item{term}{Character scalar naming the ANOVA term to test, e.g.
\code{"group:time"}. Interaction terms are order-insensitive; \code{"time:group"}
resolves to \code{"group:time"} when that is the design's factor order.}

\item{target_pes}{Target partial eta squared for \code{term}.}

\item{power}{Desired target power.}

\item{n_sims}{Number of simulated datasets per sample size.}

\item{alpha}{Significance threshold.}

\item{n_start}{Starting sample size per between-subject cell. If \code{NULL},
starts at the smallest value that can support empirical calibration for
the requested design.}

\item{n_max}{Maximum sample size per between-subject cell.}

\item{tol}{Stop when estimated power is within \code{tol} of \code{power}.}

\item{gpower}{Logical; if \code{TRUE}, calibrate means to the G*Power-style
noncentrality convention \code{lambda = total_n * f^2}. The default \code{FALSE}
calibrates the empirical reference dataset to \code{target_pes}, equivalent to
\code{lambda = den_df * f^2} for the fitted ANOVA.}

\item{progress}{Logical; if \code{TRUE}, show a text progress bar.}

\item{parallel}{Logical; if \code{TRUE}, run simulations for each sample size via
the \code{future} ecosystem.}

\item{cores}{Optional positive integer number of cores to use when
\code{parallel = TRUE}. If \code{NULL}, uses one fewer than the number of available
cores, with a minimum of one.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
An \code{anovapowersim_curve} object with \code{n_needed} and
\code{total_n_needed}.
}
\description{
Adaptive simulation search for the per-between-cell sample size needed to
reach a requested power for a balanced factorial ANOVA design. The search
doubles upward from \code{n_start} until it brackets the target or reaches
\code{n_max}, then bisects the bracket.
}
\section{Examples}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{power_n(
  between = c(cond = 2),
  within = c(stim = 4),
  term = "cond:stim",
  target_pes = 0.14,
  alpha = 0.05,
  power = 0.80,
  n_sims = 1000, # use 5000+ for a more precise estimate
  seed = 123 # for reproducibility
)
}\if{html}{\out{</div>}}
}

