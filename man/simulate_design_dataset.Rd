% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_curve.R
\name{simulate_design_dataset}
\alias{simulate_design_dataset}
\title{Simulate data from a balanced ANOVA design}
\usage{
simulate_design_dataset(design, n, means, sd = 1, r = 0.5, empirical = FALSE)
}
\arguments{
\item{design}{An \code{anovapowersim_design_spec} from \code{\link[=balanced_anova_design]{balanced_anova_design()}}.}

\item{n}{Sample size per between-subject cell. For pure within designs, this
is the total sample size.}

\item{means}{Numeric matrix of population cell means.}

\item{sd}{Common outcome standard deviation.}

\item{r}{Compound-symmetric correlation among within-subject cells.}

\item{empirical}{Logical; if \code{TRUE}, use \code{MASS::mvrnorm(empirical = TRUE)}
so the generated sample closely matches the requested means/covariance.}
}
\value{
A tibble ready for \code{stats::aov()} with columns \code{id}, factor
columns, and \code{value}.
}
\description{
Generates one long-format dataset from a balanced design. Supply means from
\code{\link[=design_term_means]{design_term_means()}} or any conformable matrix with one row per
between-subject cell and one column per within-subject cell.
}
\section{Covariance limitation}{

This manual helper does not accept \code{\link[=within_covariance]{within_covariance()}} specifications. It
always simulates from the compound-symmetric covariance defined by \code{sd} and
\code{r}. It therefore cannot reproduce a balanced power-function call that uses
a custom \code{covariance}; use the power functions directly for that workflow.
}

\examples{
d <- balanced_anova_design(between = c(group = 2), within = c(time = 2))
m <- design_term_means(d, term = "group:time", target_pes = 0.2, n = 20)
sim <- simulate_design_dataset(d, n = 20, means = m)
head(sim)

}
