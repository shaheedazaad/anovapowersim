% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/within_covariance.R
\name{within_covariance}
\alias{within_covariance}
\title{Specify a within-subject covariance structure}
\usage{
within_covariance(
  default_sd = 1,
  default_correlation = 0.5,
  standard_deviations = NULL,
  correlations = NULL
)
}
\arguments{
\item{default_sd}{Positive finite standard deviation used for measurements
not listed in \code{standard_deviations}.}

\item{default_correlation}{Finite correlation in \verb{(-1, 1)} used for pairs
not listed in \code{correlations}.}

\item{standard_deviations}{Optional named numeric vector of measurement-
specific standard deviations. Names must match cells generated by the
\code{within} design.}

\item{correlations}{Optional named numeric vector of pair-specific
correlations. Name each pair as \code{"cell1:cell2"}, for example
\code{"time1:time2" = 0.7}. Pair order does not matter.}
}
\value{
An \code{anovapowersim_covariance_spec} object. \code{\link[=power_n]{power_n()}} and
\code{\link[=power_curve]{power_curve()}} resolve it against their \code{within} design and construct the
covariance matrix as \verb{D R D}, where \code{D} contains the standard deviations
and \code{R} is the correlation matrix. They also derive the tested term's
population Greenhouse--Geisser epsilon from the resolved matrix and apply
it to \code{power_calc}.
}
\description{
Creates a readable covariance specification for \code{\link[=power_n]{power_n()}} and
\code{\link[=power_curve]{power_curve()}}. Supply default standard deviations and correlations, then
override only the individual measurements or pairs that differ. Measurement
names are generated from the \code{within} design; for example,
\code{within = c(time = 2, condition = 2)} creates \code{time1_condition1},
\code{time1_condition2}, \code{time2_condition1}, and \code{time2_condition2}.
}
\examples{
covariance <- within_covariance(
  default_sd = 1,
  default_correlation = 0.5,
  standard_deviations = c(
    "time3_condition2" = 1.2
  ),
  correlations = c(
    "time1_condition1:time1_condition2" = 0.6,
    "time2_condition1:time2_condition2" = 0.7
  )
)

\donttest{
power_n(
  within = c(time = 3, condition = 2),
  term = "time:condition",
  target_pes = 0.14,
  power = 0.90,
  n_sims = 100,
  covariance = covariance,
  seed = 123
)
}

}
