% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_effect.R
\name{power_achieved}
\alias{power_achieved}
\title{Estimate achieved ANOVA power at a fixed sample size}
\usage{
power_achieved(
  between = NULL,
  within = NULL,
  term,
  target_pes,
  n,
  n_sims = 10000,
  alpha = 0.05,
  ss_type = "III",
  gpower = FALSE,
  progress = interactive(),
  parallel = FALSE,
  cores = NULL,
  seed = NULL,
  covariance = NULL,
  means_pattern = NULL
)
}
\arguments{
\item{between}{Named integer vector of between-subject factor level counts,
e.g. \code{c(group = 2)}. Use \code{NULL} for no between-subject factors.}

\item{within}{Named integer vector of within-subject factor level counts,
e.g. \code{c(time = 3, condition = 4)}. Use \code{NULL} for no within-subject
factors.}

\item{term}{Character scalar naming the ANOVA term to test, e.g.
\code{"group:time"}. Interaction terms are order-insensitive; \code{"time:group"}
resolves to \code{"group:time"} when that is the design's factor order.}

\item{target_pes}{Target partial eta squared for \code{term}.}

\item{n}{Number of subjects per between-subject cell. For a purely
within-subject design, this is the total sample size.}

\item{n_sims}{Number of simulated datasets per sample size.}

\item{alpha}{Significance threshold.}

\item{ss_type}{Sums-of-squares type for the tested ANOVA term. \code{"III"} is
the default for order-invariant tests in unbalanced designs. Use \code{"I"} to
reproduce sequential \code{stats::aov()} tests. Greenhouse--Geisser-corrected
simulated p-values (see \code{covariance}) are only available for \code{"III"} and
\code{"II"}; under \code{"I"}, simulated p-values always use the uncorrected
univariate test, and a warning is issued if the supplied covariance
yields a population epsilon below \code{1}.}

\item{gpower}{Logical; if \code{TRUE}, calibrate means to the G\emph{Power-style
noncentrality convention \code{lambda = total_n * f^2}. The default \code{FALSE}
calibrates the empirical reference dataset to \code{target_pes}, equivalent to
\code{lambda = den_df * f^2} for the fitted ANOVA. G}Power's estimates can
differ from \code{target_pes}, especially for small samples or terms with more
degrees of freedom; a warning is issued when \code{gpower = TRUE}. The default
\code{gpower = FALSE} is recommended.}

\item{progress}{Logical; if \code{TRUE}, show a text progress bar.}

\item{parallel}{Logical; if \code{TRUE}, run simulations for each sample size via
the \code{future} ecosystem.}

\item{cores}{Optional positive integer number of cores to use when
\code{parallel = TRUE}. If \code{NULL}, uses one fewer than the number of available
cores, with a minimum of one.}

\item{seed}{Optional integer seed for reproducibility.}

\item{covariance}{Optional within-subject covariance specification from
\code{\link[=within_covariance]{within_covariance()}} or a numeric covariance matrix. The default \code{NULL}
uses standard deviations of \code{1} and a compound-symmetric correlation of
\code{0.5} and issues a warning stating those defaults. A
\code{\link[=within_covariance]{within_covariance()}} specification issues a warning when correlation
pairs are omitted: its \code{default_correlation} applies only to those
undefined pairs, while explicitly defined correlations are unchanged.
All measurements must have one common marginal variance. A supplied matrix
must therefore have equal diagonal entries and one row and column per
within-subject cell; named matrices are reordered to the design's cell
order. Unequal correlations remain supported. For terms
containing within-subject factors, the matrix is also used to derive a
term-specific population Greenhouse--Geisser epsilon for \code{power_calc}. If
that population epsilon is below \code{1}, \code{power_sim} is also based on each
simulated dataset's Greenhouse--Geisser-corrected p-value (from
\code{car::Anova()}) rather than the uncorrected univariate test, so \code{power_sim}
and \code{power_calc} estimate the same corrected test.}

\item{means_pattern}{Optional relative cell-mean shape created by
\code{\link[=means_pattern]{means_pattern()}}. The sparse values are projected onto \code{term}, normalized,
and uniformly rescaled to reach \code{target_pes}. If \code{NULL}, simulations use
the package's deterministic linear/Kronecker pattern. For multi-df
nonspherical within-subject terms, simulated power is conditional on this
direction, so an explicit pattern is recommended when the expected shape
is known.}
}
\value{
An \code{anovapowersim_achieved_power} object. \verb{$results} contains the
standard one-row power diagnostics. \verb{$achieved_power} is the simulated
power estimate and \verb{$calculated_power} is the calculated-power diagnostic.
}
\description{
Simulates one balanced factorial ANOVA design point for a fixed partial eta
squared and sample size. The simulation estimate is the primary achieved
power result; noncentral-F calculated power is retained as a diagnostic.
}
\section{Lifecycle}{

\ifelse{html}{\out{<a href="https://lifecycle.r-lib.org/articles/stages.html#experimental"><img src="https://lifecycle.r-lib.org/articles/figures/lifecycle-experimental.svg" alt="[Experimental]"></a>}}{\strong{Experimental}}

\code{power_achieved()} is experimental and is available only in the development
version of \code{anovapowersim}. Its API and reporting format may change.
}

\examples{
\donttest{
power_achieved(
  between = c(group = 2),
  within = c(time = 2),
  term = "group:time",
  target_pes = 0.14,
  n = 20,
  n_sims = 100,
  seed = 123
)
}

}
