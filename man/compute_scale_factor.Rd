% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_scale_factor.R
\name{compute_scale_factor}
\alias{compute_scale_factor}
\title{Compute the mean-deviation scaling factor from a change in partial eta squared}
\usage{
compute_scale_factor(old_pes, new_pes)
}
\arguments{
\item{old_pes}{Numeric scalar in (0, 1), or a numeric-looking character
scalar such as \code{".310"}. The current partial eta squared for the term of
interest.}

\item{new_pes}{Numeric scalar in (0, 1), or a numeric-looking character
scalar such as \code{".200"}. The target partial eta squared.}
}
\value{
A single positive numeric value \code{k}. \code{k > 1} amplifies the
effect, \code{k < 1} shrinks it, and \code{k == 1} leaves it unchanged.
}
\description{
Given an existing partial eta squared for a term and a target partial eta
squared, returns the multiplier \code{k} that must be applied to that term's
additive contribution to the cell means in order to obtain the target effect
size under the same residual structure.
}
\details{
The derivation is straightforward: partial eta squared can be written as
\code{pes = SS_effect / (SS_effect + C)}, where \code{C} is the part of the
denominator held fixed by this package's rescaling. Thus
\code{pes / (1 - pes)} scales as the target effect's sum of squares. Scaling the
term's deviations by \code{k} scales the target effect's sum of squares by
\code{k^2}, so the required multiplier is
\deqn{k = \sqrt{ \frac{p_\text{new} / (1 - p_\text{new})}
                      {p_\text{old} / (1 - p_\text{old})} }.}
}
\examples{
compute_scale_factor(0.10, 0.05)   # shrink
compute_scale_factor(0.05, 0.10)   # amplify

}
\seealso{
\code{\link[=design_term_means]{design_term_means()}}, \code{\link[=power_curve]{power_curve()}}
}
