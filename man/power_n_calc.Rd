% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_n_calc.R
\name{power_n_calc}
\alias{power_n_calc}
\title{Calculate the sample size needed for target ANOVA power}
\usage{
power_n_calc(
  between = NULL,
  within = NULL,
  term,
  target_pes,
  power = 0.9,
  alpha = 0.05,
  n_start = NULL,
  n_max = 5000,
  gpower = FALSE,
  epsilon = 1
)
}
\arguments{
\item{between}{Named integer vector of between-subject factor level counts,
e.g. \code{c(group = 2)}. Use \code{NULL} for no between-subject factors.}

\item{within}{Named integer vector of within-subject factor level counts,
e.g. \code{c(time = 3, condition = 4)}. Use \code{NULL} for no within-subject
factors.}

\item{term}{Character scalar naming the ANOVA term to test, e.g.
\code{"group:time"}. Interaction terms are order-insensitive; \code{"time:group"}
resolves to \code{"group:time"} when that is the design's factor order.}

\item{target_pes}{Target partial eta squared for \code{term}.}

\item{power}{Desired target power.}

\item{alpha}{Significance threshold.}

\item{n_start}{Starting sample size per between-subject cell. If \code{NULL},
starts from the smallest value with valid calculated-power degrees of
freedom.}

\item{n_max}{Maximum sample size per between-subject cell.}

\item{gpower}{Logical; if \code{TRUE}, use the G*Power-style noncentrality
convention \code{lambda = total_n * f^2}. The default \code{FALSE} uses
\code{lambda = den_df * f^2}.}

\item{epsilon}{Population nonsphericity correction for the within-subject
component of \code{term}. Must lie between the theoretical lower bound
\code{1 / within_term_df} and \code{1}. The default \code{1} assumes sphericity. Values
below \code{1} multiply the numerator degrees of freedom, denominator degrees
of freedom, and noncentrality parameter. Nonsphericity corrections do not
apply to purely between-subject terms.}
}
\value{
An \code{anovapowersim_curve} object with \code{n_needed} and
\code{total_n_needed}. The \verb{$results} tibble contains \code{n_per_cell}, \code{total_n},
\code{n_sims}, numerator and denominator degrees of freedom (\code{num_df},
\code{den_df}), the nonsphericity correction (\code{epsilon}), the noncentrality
parameter (\code{ncp}), calculated power (\code{power_calc}), and simulated power
(\code{power_sim}). For \code{power_n_calc()}, \code{n_sims} and \code{power_sim} are always
\code{NA}. When \code{epsilon < 1}, \code{num_df} and \code{den_df} are the corrected degrees
of freedom used in the power calculation.
}
\description{
Calculation-only search for the per-between-cell sample size needed to reach
a requested power for a balanced factorial ANOVA design. Unlike
\code{\link[=power_n]{power_n()}}, this function does not run simulations, fit ANOVA models, or
call \code{car}; numerator degrees of freedom, denominator degrees of freedom,
noncentrality, and calculated power are obtained directly from the balanced
design.
}
\section{Lifecycle}{

\ifelse{html}{\out{<a href="https://lifecycle.r-lib.org/articles/stages.html#experimental"><img src="https://lifecycle.r-lib.org/articles/figures/lifecycle-experimental.svg" alt="[Experimental]"></a>}}{\strong{Experimental}}

\code{power_n_calc()} is experimental while the calculated-power search API and
reporting format are refined.
}

\examples{
power_n_calc(
  between = c(cond = 2),
  within = c(stim = 4),
  term = "cond:stim",
  target_pes = 0.14,
  power = 0.90,
  epsilon = 0.70
)

}
