% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_unbalanced.R
\name{power_unbalanced}
\alias{power_unbalanced}
\title{Simulate power for a fixed unbalanced ANOVA design}
\usage{
power_unbalanced(
  design,
  term,
  covariance = NULL,
  n_sims = 10000,
  alpha = 0.05,
  ss_type = "III",
  progress = interactive(),
  parallel = FALSE,
  cores = NULL,
  seed = NULL,
  sim_correction = c("auto", "GG", "none")
)
}
\arguments{
\item{design}{A complete design table created by \code{\link[=cell_design]{cell_design()}}.
Within-subject factors are read from the design's \code{within} attribute
(set via \code{\link[=cell_design]{cell_design()}}'s \code{within} argument), not supplied here.}

\item{term}{Character scalar naming the ANOVA term to test.}

\item{covariance}{Optional common covariance specification created by
\code{\link[=unbalanced_covariance]{unbalanced_covariance()}}. The default \code{NULL} uses \code{sd = 1} and a
correlation of \code{0.5} between within-subject measurements and issues a
warning stating those defaults. For purely between-subject designs, use
this argument to change the common SD; correlation settings are not
applicable. When an \code{\link[=unbalanced_covariance]{unbalanced_covariance()}} specification omits some
correlation pairs, a warning states that \code{default_correlation} is used
only for those undefined pairs. The resolved covariance determines the
term-specific population Greenhouse--Geisser epsilon used by
\code{sim_correction = "auto"}.}

\item{n_sims}{Number of simulated datasets.}

\item{alpha}{Significance threshold.}

\item{ss_type}{Sums-of-squares type: \code{"III"}, \code{"II"}, or \code{"I"}.
For unequal-N designs, \code{"I"} uses sequential, order-dependent hypotheses;
a warning reports the factor order inherited from \code{\link[=cell_design]{cell_design()}}.}

\item{progress}{Logical; if \code{TRUE}, show a text progress bar.}

\item{parallel}{Logical; if \code{TRUE}, run simulations in parallel.}

\item{cores}{Optional positive integer number of parallel workers.}

\item{seed}{Optional integer seed for reproducibility.}

\item{sim_correction}{Sphericity correction for simulated p-values:
\code{"auto"} (the default) uses Greenhouse--Geisser correction when the
term-specific population epsilon is below \code{1 - 1e-8} and \code{ss_type} is
\code{"II"} or \code{"III"}; \code{"GG"} requests correction for every simulated
dataset; and \code{"none"} always uses the uncorrected univariate test.
\code{"GG"} is an error with \code{ss_type = "I"}. For a between-only term or a
within component with one degree of freedom, \code{"GG"} silently resolves to
\code{"none"} because no sphericity correction applies.}
}
\value{
An \code{anovapowersim_unbalanced_power} object. \verb{$power} and
\verb{$achieved_power} contain simulated power. \verb{$partial_eta_squared} is the
term effect size in a deterministic reference dataset. \verb{$epsilon} is the
population Greenhouse--Geisser epsilon for the tested term. \verb{$results}
also reports the common SD, simulated partial eta-squared distribution,
and failed fits. Sample partial eta squared is upward-biased in finite
samples, so \code{mean_pes_sim}, \code{median_pes_sim}, and the simulated interval
are sampling diagnostics rather than estimates of the supplied population
effect; use \verb{$partial_eta_squared} as the reference effect. The object
stores the requested \code{sim_correction} and applied
\code{sim_correction_resolved} values.
}
\description{
Estimates achieved power for exact, potentially unequal cell sizes and
user-supplied population means under one common standard deviation. This
function is simulation-only: it does not calculate power from a noncentral
F distribution and does not scale the supplied sample sizes. A warning is
issued when the deterministic reference data imply essentially zero effect
for the tested term, which often indicates a typo, a wrong \code{term}, or means
that contain only other effects.
}
\section{Lifecycle}{

\ifelse{html}{\out{<a href="https://lifecycle.r-lib.org/articles/stages.html#experimental"><img src="https://lifecycle.r-lib.org/articles/figures/lifecycle-experimental.svg" alt="[Experimental]"></a>}}{\strong{Experimental}}

\code{power_unbalanced()} is experimental and is available only in the
development version of \code{anovapowersim}. Its API and reporting format may
change.
}

\section{Simulated sphericity correction}{

\code{sim_correction} governs only the simulated test. With \code{"GG"}, each dataset
uses its own sample-estimated Greenhouse--Geisser epsilon from
\code{car::Anova()}. Under a truly spherical population, that sample correction
is mildly conservative. Power is estimated for the prespecified corrected
or uncorrected test; conditional Mauchly-then-correct procedures are not
simulated. Unlike the balanced simulation functions, \code{power_unbalanced()}
does not report a \code{power_calc} diagnostic.
}

\examples{
\donttest{
design <- cell_design(
  group = "control", time = "pre",  n = 12, m = 10,
  group = "control", time = "post", n = 12, m = 11,
  group = "treatment", time = "pre",  n = 18, m = 10,
  group = "treatment", time = "post", n = 18, m = 13,
  within = "time"
)
power_unbalanced(
  design = design,
  term = "group:time",
  covariance = unbalanced_covariance(
    sd = 2,
    correlations = c("pre:post" = 0.7)
  ),
  n_sims = 100,
  seed = 123
)
}

}
