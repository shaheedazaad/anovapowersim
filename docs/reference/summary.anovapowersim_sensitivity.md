# Summarise a fixed-sample sensitivity result

Summarise a fixed-sample sensitivity result

## Usage

``` r
# S3 method for class 'anovapowersim_sensitivity'
summary(object, ...)
```

## Arguments

- object:

  An `anovapowersim_sensitivity` object.

- ...:

  Unused.

## Value

A list with `header` and `results`, invisibly; printed to the console as
well.
